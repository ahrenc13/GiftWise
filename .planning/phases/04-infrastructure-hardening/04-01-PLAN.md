---
phase: 04-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skimlinks_searcher.py
  - templates/base.html
  - giftwise_app.py
  - multi_retailer_searcher.py
  - recommendation_service.py
  - revenue_optimizer.py
  - image_fetcher.py
  - product_schema.py
  - models.py
  - config.py
  - config_service.py
  - config/settings.py
  - products/ingestion.py
autonomous: true
requirements:
  - INF-01
  - INF-02

must_haves:
  truths:
    - "`skimlinks_searcher.py` does not exist in the repo"
    - "No page served through `base.html` loads Skimlinks JS (publisher ID `298548X178612` absent from all templates)"
    - "No Python file imports from `skimlinks_searcher` (file or runtime import)"
    - "The app starts and handles requests without referencing the deleted module"
  artifacts:
    - path: "skimlinks_searcher.py"
      provides: "DELETED — must not exist"
    - path: "templates/base.html"
      provides: "Skimlinks JS snippet removed (lines 336-346)"
    - path: "giftwise_app.py"
      provides: "Env var constant + URL-wrapping block removed"
    - path: "multi_retailer_searcher.py"
      provides: "Skimlinks params, comments, and `_run_skimlinks` closure removed"
    - path: "recommendation_service.py"
      provides: "Instance var, param forwarding, URL-wrapping block removed"
    - path: "products/ingestion.py"
      provides: "`refresh_skimlinks()` function and all retailer list entries removed"
  key_links:
    - from: "multi_retailer_searcher.py"
      to: "skimlinks_searcher.py"
      via: "runtime import inside `_run_skimlinks` closure (line 303)"
      pattern: "from skimlinks_searcher import"
    - from: "products/ingestion.py"
      to: "skimlinks_searcher.py"
      via: "try/except import at lines 47-49"
      pattern: "import skimlinks_searcher"
---

<objective>
Remove all dead Skimlinks code from the GiftWise codebase. Skimlinks is a defunct service — the API no longer functions, the env vars are not set in Railway, and the code paths never execute in production. The code creates confusion for future maintainers and the JS snippet fires a broken network request on every page load.

Purpose: Eliminate dead code that obscures the active retailer architecture and fires a pointless external script request on every page.
Output: Deleted `skimlinks_searcher.py`, cleaned templates, and 12 Python files with all Skimlinks constants/functions/imports removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-infrastructure-hardening/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete skimlinks_searcher.py and remove primary references in giftwise_app.py and base.html</name>
  <files>skimlinks_searcher.py, templates/base.html, giftwise_app.py</files>
  <action>
**Step 1: Delete the file.**
```bash
rm /home/user/GiftWise/skimlinks_searcher.py
```

**Step 2: Remove Skimlinks JS snippet from `templates/base.html`.**
Read the file first. Remove lines 336-346 (the comment `<!-- Skimlinks snippet - Publisher ID: 298548X178612 -->` and the `<script>` async loader block). The Sovrn comment block immediately after (lines ~348-355) is unrelated — leave it.
Do NOT leave a stray `</script>` tag or break surrounding HTML structure.

**Step 3: Remove Skimlinks references from `giftwise_app.py`.**
Read the file. Make these targeted removals:
- Line 466: Remove `SKIMLINKS_PUBLISHER_ID = os.environ.get('SKIMLINKS_PUBLISHER_ID', '')` (the module-level constant).
- Lines 3019 (comment mentioning Skimlinks) + 3031-3033 (the URL-wrapping block): Remove the `# Step 2: Skimlinks server-side wrapping (when approved)` comment and the `if SKIMLINKS_PUBLISHER_ID and not url.startswith(...)` block inside `_apply_affiliate_tag()`. The surrounding code (step 1 comment and the return statement) should remain intact.

After editing, confirm `giftwise_app.py` has zero occurrences of `SKIMLINKS_PUBLISHER_ID` and zero occurrences of `go.skimresources.com`.
  </action>
  <verify>
    <automated>
      cd /home/user/GiftWise && python -c "import ast; ast.parse(open('giftwise_app.py').read()); print('giftwise_app.py: valid syntax')" && grep -rn "skimlinks\|skimresources\|298548X178612" templates/ && echo "FAIL: templates still contain Skimlinks" || echo "templates: clean" && [ ! -f skimlinks_searcher.py ] && echo "skimlinks_searcher.py: deleted" || echo "FAIL: file still exists"
    </automated>
  </verify>
  <done>
    - `skimlinks_searcher.py` does not exist
    - `templates/base.html` contains no `skimlinks`, `skimresources`, or `298548X178612` strings
    - `giftwise_app.py` has no `SKIMLINKS_PUBLISHER_ID` constant and no `go.skimresources.com` URL construction
    - `giftwise_app.py` passes Python syntax check
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and clean the 12 remaining Python files with Skimlinks references</name>
  <files>multi_retailer_searcher.py, recommendation_service.py, revenue_optimizer.py, image_fetcher.py, product_schema.py, models.py, config.py, config_service.py, config/settings.py, products/ingestion.py</files>
  <action>
Work through each file. Read it, make targeted removals, verify syntax. Exact changes per file (line numbers are approximate — read before editing):

**`multi_retailer_searcher.py`:**
- Remove the 4 Skimlinks keyword parameters from the `search_all_retailers()` function signature (lines ~47-50): `skimlinks_publisher_id`, `skimlinks_client_id`, `skimlinks_client_secret`, `skimlinks_domain_id`.
- Remove the comment and logging lines that mention Skimlinks (lines ~66, ~77).
- Remove the entire `_run_skimlinks` conditional closure block (lines ~301-315): the guard `if skimlinks_publisher_id:`, the inner `def _run_skimlinks():` function (which does the runtime import), and any thread submission for it.

**`recommendation_service.py`:**
- Remove line ~57: `self.skimlinks_publisher_id = os.environ.get('SKIMLINKS_PUBLISHER_ID', '')` from `__init__`.
- Remove lines ~362-365: the 4 Skimlinks keyword arguments passed to `search_all_retailers()`.
- Remove lines ~1204-1206: the identical URL-wrapping block (`if self.skimlinks_publisher_id and not url.startswith(...)`). This is a duplicate of the block already removed from `giftwise_app.py`.

**`revenue_optimizer.py`:**
- Remove line ~30: `'skimlinks': 0.04` from the commission rates dict.

**`image_fetcher.py`:**
- Remove line ~411: `'skimlinks'` entry from the supported retailer list.
- Remove line ~441: `'skimlinks': _extract_skimlinks_image` from the dispatcher dict.
- Remove lines ~559-580+: the entire `_extract_skimlinks_image()` function.

**`product_schema.py`:**
- Remove line ~86: `'skimlinks': 0.03` from the commission rates dict.
- Remove lines ~419-463: the entire `Product.from_skimlinks()` classmethod.
- Remove lines ~531, ~553: `'skimlinks'` entries from the platform list and dispatcher dict.

**`models.py`:**
- Line ~32: Remove `'skimlinks'` from the comment in the retailer type annotation. This is a comment only — update to plaintext noting Skimlinks was removed.

**`config.py`:**
- Remove lines ~63-67: the 4 Skimlinks env var reads (`SKIMLINKS_PUBLISHER_ID`, `SKIMLINKS_CLIENT_ID`, `SKIMLINKS_CLIENT_SECRET`, `SKIMLINKS_PUBLISHER_DOMAIN_ID`).
- Remove line ~132: the Skimlinks priority weight entry.
- Remove line ~379: the Skimlinks product limit entry.
- Remove lines ~423-424: the Skimlinks warning log block.
- Remove line ~559: the Skimlinks status print.

**`config_service.py`:**
- Remove lines ~107-111: the 4 Skimlinks fields from the config dataclass.
- Remove lines ~151-155: the 4 Skimlinks env var reads.
- Remove line ~182: `'skimlinks'` from the available retailers check/list.

**`config/settings.py`:**
- Remove lines ~57-61: the 4 Skimlinks config fields.
- Remove line ~162: `'skimlinks'` from the active retailers list.

**`products/ingestion.py`:**
- Remove lines ~47-49: the try/except import of `skimlinks_searcher` (`try: import skimlinks_searcher ... except ImportError: skimlinks_searcher = None`).
- Remove lines ~330-382: the entire `refresh_skimlinks()` function.
- Remove lines ~451, ~466, ~509, ~534, ~554: all `'skimlinks'` entries from retailer choice lists and result dicts.

**After all edits, verify the codebase is clean:**
```bash
grep -rn "skimlinks\|skimresources\|SKIMLINKS" /home/user/GiftWise/ \
  --include="*.py" --include="*.html" \
  --exclude-dir=.git \
  --exclude="scripts/convert_templates.py" \
  --exclude="scripts/verify_templates.py"
```
Expected output: zero matches (or only matches in the two excluded scripts, which are harmless migration utilities).
  </action>
  <verify>
    <automated>
      cd /home/user/GiftWise && python -c "
import ast, os, sys
failures = []
files = [
    'multi_retailer_searcher.py', 'recommendation_service.py', 'revenue_optimizer.py',
    'image_fetcher.py', 'product_schema.py', 'models.py', 'config.py',
    'config_service.py', 'config/settings.py', 'products/ingestion.py'
]
for f in files:
    try:
        ast.parse(open(f).read())
    except SyntaxError as e:
        failures.append(f'{f}: {e}')
if failures:
    print('SYNTAX ERRORS:')
    for f in failures: print(f)
    sys.exit(1)
else:
    print('All files: valid syntax')
" && grep -rn "skimlinks\|skimresources\|SKIMLINKS" . --include="*.py" --include="*.html" --exclude-dir=.git --exclude="scripts/convert_templates.py" --exclude="scripts/verify_templates.py" && echo "FAIL: Skimlinks references remain" || echo "No Skimlinks references found — clean"
    </automated>
  </verify>
  <done>
    - All 10 edited Python files pass `ast.parse()` syntax check
    - Zero occurrences of `skimlinks`, `skimresources`, or `SKIMLINKS` in any `.py` or `.html` file (excluding the two harmless migration scripts in `scripts/`)
    - `products/ingestion.py` has no import of `skimlinks_searcher` (try/except or otherwise)
    - `multi_retailer_searcher.py` has no `_run_skimlinks` closure and no `from skimlinks_searcher import`
  </done>
</task>

</tasks>

<verification>
Final check after both tasks complete:

```bash
cd /home/user/GiftWise && \
  echo "=== File deleted ===" && \
  [ ! -f skimlinks_searcher.py ] && echo "OK: skimlinks_searcher.py absent" || echo "FAIL" && \
  echo "=== No remaining references ===" && \
  grep -rn "skimlinks\|skimresources\|SKIMLINKS\|298548X178612" . \
    --include="*.py" --include="*.html" --exclude-dir=.git \
    --exclude="scripts/convert_templates.py" \
    --exclude="scripts/verify_templates.py" \
    && echo "FAIL: references remain" || echo "OK: all references removed" && \
  echo "=== App imports OK ===" && \
  python -c "import giftwise_app; print('OK: app imports without error')" 2>&1 | head -5
```
</verification>

<success_criteria>
1. `skimlinks_searcher.py` is deleted (INF-01)
2. `templates/base.html` and all other templates contain no `skimlinks`, `skimresources`, or `298548X178612` strings (INF-02)
3. Zero Python files contain `from skimlinks_searcher import` or `import skimlinks_searcher`
4. All modified Python files pass syntax check
5. `giftwise_app.py` imports cleanly (no NameError from removed constant)
</success_criteria>

<output>
After completion, create `.planning/phases/04-infrastructure-hardening/04-01-SUMMARY.md` with:
- What was removed (file counts, function names)
- Any references intentionally left (explain why)
- Verification output confirming zero remaining Skimlinks references
</output>
