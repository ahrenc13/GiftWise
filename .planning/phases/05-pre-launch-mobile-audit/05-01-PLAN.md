---
phase: 05-pre-launch-mobile-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/recommendations.html
autonomous: true
requirements:
  - LAUNCH-01
must_haves:
  truths:
    - "Every tappable button on the recommendations page meets the 44px minimum touch target"
    - "The actions-section does not feel cramped on a 375px-wide phone"
  artifacts:
    - path: "templates/recommendations.html"
      provides: "Updated CSS for touch targets and actions-section mobile padding"
      contains: "min-height: 44px"
  key_links:
    - from: "templates/recommendations.html CSS"
      to: "rendered recommendation cards on mobile"
      via: "Jinja2 template render"
      pattern: "min-height: 44px"
---

<objective>
Fix five undersized touch targets in recommendations.html and add a mobile override for the actions-section padding. All changes are CSS-only surgical edits — no layout restructuring, no JS changes.

Purpose: TikTok visitors land on mobile. Buttons that are 36-40px tall miss the 44px Apple/Material minimum and cause mis-taps. Fixing this before traffic arrives eliminates a friction point that hurts conversion.

Output: Updated templates/recommendations.html with corrected touch target heights and a tighter mobile padding override for the actions-section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

The five touch target issues to fix (from 05-RESEARCH.md, Q4 findings):

| Issue | Selector | Current | Fix |
|-------|----------|---------|-----|
| M2 | `.rec-link` | ~40px (padding: 12px 24px) | Add min-height: 44px; display: flex; align-items: center; justify-content: center |
| M3 | `.filter-button` | ~36px (padding: 10px 20px) | Add min-height: 44px |
| M4 | `.card-close-btn` | 36x36px explicit | Change to width: 44px; height: 44px; min-width: 44px; min-height: 44px |
| M5 | `.favorite-btn` | no sizing CSS at all | Add min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center |
| M7 | `.actions-section` | padding: 50px 40px, no mobile override | Add @media override: padding: 32px 20px |

Line references from research (confirm by reading file before editing):
- `.filter-button`: lines 89-99
- `.rec-link`: lines 524-533
- `.actions-section`: lines 557-564
- `.card-close-btn`: lines 586-602
- `.favorite-btn`: line 792-795
- Existing `@media (max-width: 768px)` block: lines 608-626
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix touch targets for .rec-link, .filter-button, .card-close-btn, .favorite-btn</name>
  <files>templates/recommendations.html</files>
  <action>
Read templates/recommendations.html fully before making any changes. Locate each selector below and apply the specified CSS addition. Make all four changes in a single editing pass.

**Selector 1 — `.rec-link`** (around line 524):
Add to existing rule: `min-height: 44px; display: flex; align-items: center; justify-content: center;`
Reason: effective height is ~40px without this; display:flex needed so align-items works on an anchor element.
Do NOT change font-size, padding, color, or border-radius.

**Selector 2 — `.filter-button`** (around line 89):
Add to existing rule: `min-height: 44px;`
Reason: effective height is ~36px.
Do NOT change any other property.

**Selector 3 — `.card-close-btn`** (around lines 591-592):
The rule currently sets `width: 36px; height: 36px`. Change those two lines to: `width: 44px; height: 44px; min-width: 44px; min-height: 44px;`
Reason: was explicitly sized at 36px, below Apple HIG minimum.
Do NOT change position, background, border-radius, z-index, or any other property.

**Selector 4 — `.favorite-btn`** (around line 792):
If there is no existing CSS rule for `.favorite-btn`, add one to the `<style>` block near the other button rules. Add: `min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; padding: 0; background: transparent; border: none; cursor: pointer;`
Reason: the heart emoji glyph is the only tap surface — it is effectively ~16px tall.

Do NOT modify `.action-button` — research confirmed it is already 48px (OK). Do NOT touch any JS or Jinja2 template logic.
  </action>
  <verify>
    <automated>grep -c "min-height: 44px" /home/user/GiftWise/templates/recommendations.html</automated>
  </verify>
  <done>At least 4 occurrences of `min-height: 44px` appear in the file (.rec-link, .filter-button, .card-close-btn or via min-height, .favorite-btn). The .card-close-btn width/height reads 44px.</done>
</task>

<task type="auto">
  <name>Task 2: Add mobile padding override for .actions-section</name>
  <files>templates/recommendations.html</files>
  <action>
The `.actions-section` currently has `padding: 50px 40px` with no mobile override. On a 375px phone this leaves only 295px of usable width — visibly tight.

Find the existing `@media (max-width: 768px)` block (around lines 608-626). Append the following rule inside that block, after the existing `.header` rule:

```css
.actions-section {
    padding: 32px 20px;
}
```

Why these values: 20px horizontal matches the `.container`'s own horizontal padding so section content aligns cleanly with the page edge. 32px vertical maintains breathing room without the excessive 50px desktop spacing.

Do NOT create a second `@media (max-width: 768px)` block — add inside the existing one.
Do NOT change any other rules in the media query block.
  </action>
  <verify>
    <automated>grep -n "actions-section" /home/user/GiftWise/templates/recommendations.html</automated>
  </verify>
  <done>grep shows two entries for actions-section: the original `padding: 50px 40px` desktop rule and a `padding: 32px 20px` override inside the `@media (max-width: 768px)` block.</done>
</task>

</tasks>

<verification>
After both tasks, confirm the template still parses:

```bash
cd /home/user/GiftWise && python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('templates'))
env.get_template('recommendations.html')
print('Template parses OK')
"
```

If Jinja2 raises TemplateSyntaxError, find and fix the broken tag before marking done.

Also spot-check touch target CSS:
```bash
grep -n "min-height: 44px" /home/user/GiftWise/templates/recommendations.html
grep -n "width: 44px; height: 44px" /home/user/GiftWise/templates/recommendations.html
```
</verification>

<success_criteria>
1. `.rec-link` has `min-height: 44px` and flex centering
2. `.filter-button` has `min-height: 44px`
3. `.card-close-btn` is 44x44px (not 36x36px)
4. `.favorite-btn` has `min-height: 44px` and flex centering
5. `.actions-section` has a `@media (max-width: 768px)` override with `padding: 32px 20px`
6. Template renders without Jinja2 syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-pre-launch-mobile-audit/05-01-SUMMARY.md` documenting:
- What was changed and at which lines
- Before/after CSS values for each selector
- Output of the Jinja2 parse check
- Any edge cases observed
</output>
