---
phase: 03-14-item-output-sonnet-portions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - multi_retailer_searcher.py
  - recommendation_service.py
  - gift_curator.py
  - giftwise_app.py
autonomous: true
requirements:
  - OUT-01
  - OUT-02

must_haves:
  truths:
    - "splurge_candidates flows as a separate list from DB through searcher, recommendation_service, and into curator inventory block"
    - "curate_gifts() accepts splurge_candidates parameter and appends them to inventory block"
    - "splurge_ceiling integer is computed from budget_category and passed to recommendations template"
    - "Existing recommendation functionality is not broken — 10 regular gifts + 3 experiences still render"
  artifacts:
    - path: "multi_retailer_searcher.py"
      provides: "Dict return with splurge_candidates key"
      contains: "'splurge_candidates'"
    - path: "recommendation_service.py"
      provides: "Unpacks dict return; threads splurge_candidates to curator"
      contains: "splurge_candidates"
    - path: "gift_curator.py"
      provides: "splurge_candidates parameter; SPLURGE CANDIDATES section in inventory"
      contains: "splurge_candidates"
    - path: "giftwise_app.py"
      provides: "splurge_ceiling computation and template pass-through"
      contains: "splurge_ceiling"
  key_links:
    - from: "multi_retailer_searcher.py"
      to: "recommendation_service.py"
      via: "dict return with 'products' and 'splurge_candidates' keys"
      pattern: "splurge_candidates"
    - from: "recommendation_service.py"
      to: "gift_curator.py"
      via: "splurge_candidates kwarg to curate_gifts()"
      pattern: "splurge_candidates=splurge_candidates"
    - from: "giftwise_app.py"
      to: "templates/recommendations.html"
      via: "splurge_ceiling in render_template context"
      pattern: "splurge_ceiling="
---

<objective>
Wire splurge_candidates as a separate list through the pipeline from the DB query to the curator inventory block, and compute splurge_ceiling from budget_category for template rendering.

Purpose: The curator needs splurge candidates as a distinct section in its inventory to pick from when Opus later adds splurge slot instructions. The template needs the budget-derived ceiling for badge text.
Output: Pipeline wiring complete — data flows end-to-end. No prompt logic changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-14-item-output-sonnet-portions/03-CONTEXT.md
@.planning/phases/03-14-item-output-sonnet-portions/03-RESEARCH.md

@multi_retailer_searcher.py
@recommendation_service.py
@gift_curator.py
@giftwise_app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread splurge_candidates through searcher and recommendation_service</name>
  <files>multi_retailer_searcher.py, recommendation_service.py</files>
  <action>
**multi_retailer_searcher.py:**

1. Pre-initialize `splurge_candidates = []` alongside the existing `per_interest_counts = {}` (line ~79) — ensures variable survives if DB block fails.

2. Change the return at line ~409 from `return all_products` to:
   ```python
   return {'products': all_products, 'splurge_candidates': splurge_candidates}
   ```
   The `splurge_candidates` variable is already extracted at line ~144 (`splurge_candidates = db_result.get('splurge_candidates', [])`). Keep the existing `splurge_candidates[:5]` mixed into `all_db_rows` at line ~156 (per CONTEXT.md: "keep in regular pool AND expose separately").

3. Update the docstring (line ~70) to reflect the dict return type.

**recommendation_service.py:**

1. In `_search_products()` (line ~323), the call to `search_products_multi_retailer` at line ~342 currently expects a list. Update to unpack the dict:
   ```python
   search_result = search_products_multi_retailer(...)
   products = search_result.get('products', search_result) if isinstance(search_result, dict) else search_result
   splurge_candidates = search_result.get('splurge_candidates', []) if isinstance(search_result, dict) else []
   ```
   The isinstance check provides backward safety if the function is ever called differently. Return both from `_search_products()`.

2. In `_curate_gifts()` (line ~407), add `splurge_candidates=None` parameter. Forward it to `curate_gifts()` at line ~453 by adding `splurge_candidates=splurge_candidates` to the kwargs.

3. In `generate_recommendations()`, where `_search_products()` is called, unpack the splurge_candidates and pass them through to `_curate_gifts()`.

**CRITICAL:** Both files must be updated together — changing the return type in the searcher without updating the caller breaks `len(products)` and similar list operations in recommendation_service.py.
  </action>
  <verify>
    <automated>cd /home/user/GiftWise && python -c "
import multi_retailer_searcher as m
import inspect

# Verify return type documentation mentions dict
src = inspect.getsource(m.search_products_multi_retailer)
assert 'splurge_candidates' in src, 'splurge_candidates not in searcher function'
assert \"'products'\" in src or '\"products\"' in src, 'products key not in return dict'

# Verify pre-initialization
lines = src.split('\n')
found_preinit = any('splurge_candidates' in l and '=' in l and '[]' in l for l in lines[:30])
assert found_preinit or any('splurge_candidates = []' in l for l in lines), 'splurge_candidates not pre-initialized'

print('multi_retailer_searcher.py: OK')

import recommendation_service as rs
src2 = inspect.getsource(rs.RecommendationService)
assert 'splurge_candidates' in src2, 'splurge_candidates not in recommendation_service'
print('recommendation_service.py: OK')
print('PASS')
"
    </automated>
  </verify>
  <done>
    - `search_products_multi_retailer()` returns `{'products': [...], 'splurge_candidates': [...]}`
    - `splurge_candidates` pre-initialized to `[]` before DB try block
    - `_search_products()` unpacks the dict and returns both lists
    - `_curate_gifts()` accepts and forwards `splurge_candidates` to `curate_gifts()`
  </done>
</task>

<task type="auto">
  <name>Task 2: Add splurge_candidates to curator inventory + compute splurge_ceiling</name>
  <files>gift_curator.py, giftwise_app.py</files>
  <action>
**gift_curator.py:**

1. Add `splurge_candidates=None` to the `curate_gifts()` signature (line ~30), after the existing `ontology_briefing=None` parameter.

2. After the `products_summary` is built (line ~208 area, where the inventory block is constructed), append a SPLURGE CANDIDATES section if splurge_candidates is non-empty:
   ```python
   if splurge_candidates:
       splurge_formatted = format_products(splurge_candidates)  # Use the same format_products helper already used for regular products
       products_summary += f"\n\n━━━ SPLURGE CANDIDATES ($200-$1500) ━━━\n\n{splurge_formatted}\n\n━━━ END SPLURGE CANDIDATES ━━━"
   ```
   Use whatever `format_products` function or inline formatting the file already uses for the regular products. Place the splurge section AFTER the regular products in the inventory block.

3. Do NOT modify the existing SPLURGE PICK instruction at line ~247 or any other prompt text. Do NOT modify the JSON schema. Those are Opus-only zones.

**giftwise_app.py:**

1. Add a module-level constant near the top of the file (or near other config constants):
   ```python
   _SPLURGE_CEILING_MAP = {
       'budget': 300,
       'moderate': 500,
       'premium': 1000,
       'luxury': 1500,
   }
   ```

2. In the `view_recommendations()` function (line ~3616), before the `render_template` call at line ~3680, compute splurge_ceiling:
   ```python
   profile = (user.get('recipient_profile') or {})
   budget_cat = profile.get('price_signals', {}).get('budget_category', 'unknown')
   splurge_ceiling = _SPLURGE_CEILING_MAP.get(budget_cat, 500)
   ```

3. Add `splurge_ceiling=splurge_ceiling` to the `render_template('recommendations.html', ...)` call at line ~3680.

4. Also update the other `render_template('recommendations.html', ...)` calls — research identified them at lines ~3853 (shared recommendations), ~3896, and ~3904 (favorites view). For these, pass `splurge_ceiling=500` as a safe default (profile may not be available in those contexts). Search for ALL occurrences to be thorough.

Per CONTEXT.md: use `(user.get('recipient_profile') or {})` — the `or {}` handles None when the profile was generated before this deploy.
  </action>
  <verify>
    <automated>cd /home/user/GiftWise && python -c "
import gift_curator
import inspect

sig = inspect.signature(gift_curator.curate_gifts)
params = list(sig.parameters.keys())
assert 'splurge_candidates' in params, f'splurge_candidates not in curate_gifts params: {params}'
print('gift_curator.py: splurge_candidates param OK')

src = inspect.getsource(gift_curator.curate_gifts)
assert 'SPLURGE CANDIDATES' in src, 'SPLURGE CANDIDATES section not in inventory block'
print('gift_curator.py: inventory section OK')

# Verify giftwise_app has ceiling
with open('giftwise_app.py', 'r') as f:
    app_src = f.read()
assert '_SPLURGE_CEILING_MAP' in app_src, 'ceiling map not in giftwise_app.py'
assert 'splurge_ceiling' in app_src, 'splurge_ceiling not in giftwise_app.py'

# Count render_template calls with splurge_ceiling
import re
renders = re.findall(r'render_template\([^)]*recommendations\.html[^)]*\)', app_src, re.DOTALL)
for i, r in enumerate(renders):
    assert 'splurge_ceiling' in r, f'render_template call {i+1} missing splurge_ceiling'
print(f'giftwise_app.py: all {len(renders)} render_template calls have splurge_ceiling')
print('PASS')
"
    </automated>
  </verify>
  <done>
    - `curate_gifts()` accepts `splurge_candidates` param and appends them to the inventory block under "SPLURGE CANDIDATES ($200-$1500)" header
    - No Opus-only prompt text modified (SPLURGE PICK instruction at line ~247 untouched)
    - `_SPLURGE_CEILING_MAP` constant defined in giftwise_app.py
    - `splurge_ceiling` computed from `budget_category` and passed to ALL `render_template('recommendations.html', ...)` calls
    - Default ceiling is 500 when budget_category is missing, None, or unrecognized
  </done>
</task>

</tasks>

<verification>
1. `python -c "from gift_curator import curate_gifts; import inspect; print('splurge_candidates' in inspect.signature(curate_gifts).parameters)"` returns True
2. `grep -n 'splurge_candidates' multi_retailer_searcher.py recommendation_service.py gift_curator.py` shows matches in all 3 files
3. `grep -n 'splurge_ceiling' giftwise_app.py` shows the constant, computation, and template pass-through
4. `grep -c "splurge_ceiling" giftwise_app.py` count matches the number of render_template calls for recommendations.html
5. `python giftwise_app.py &` starts without import errors (kill after confirming startup)
</verification>

<success_criteria>
- splurge_candidates flows from DB through searcher dict return, through recommendation_service, into curator inventory block
- splurge_ceiling (integer) computed from budget_category and available in all template render calls
- No Opus-only zones modified (prompt text, JSON schema, selection principles)
- Application starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-14-item-output-sonnet-portions/03-01-SUMMARY.md`
</output>
